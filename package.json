{
  "name": "kikilink",
  "version": "0.11.0",
  "private": true,
  "description": "A polished, modular social and interaction addon for Bondage Club.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "bondage-club-mod-sdk": "1.2.0"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "esbuild": "0.28.2",
    "happy-dom": "20.11.6",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
